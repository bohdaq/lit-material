{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "@lit-material/docs#build": {
      "dependsOn": ["^build", "@lit-material/app-shell-demo#build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "env": ["WTR_BROWSERS"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "analyze": {
      "outputs": ["custom-elements.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
