import { css } from "lit";

export const styles = css`
  :host {
    display: block;
    outline: none;
  }

  .tree {
    display: flex;
    flex-direction: column;
    color: var(--md-sys-color-on-surface, #1c1b1f);
  }
`;
